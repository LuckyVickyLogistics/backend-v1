spring:
  application:
    name: product

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19094/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost  # 클라이언트 호스트 이름
    prefer-ip-address: true